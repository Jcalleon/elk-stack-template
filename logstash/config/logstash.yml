http.host: 0.0.0.0

xpack.monitoring.enabled: false

# Logstash authenticates to Elasticsearch as the dedicated
# 'logstash_internal' service account (least privilege, not 'elastic').
xpack.management.elasticsearch.hosts: ["https://elasticsearch:9200"]
